# 更新日志

## 0.2.1 (2026-08-17)

### 修复
- 同步上游 astrbot_plugin_markdown_killer 的 Markdown 剥离正则修补：图片链接 `![alt](url)` 提前处理（避免残留 `!`）、删除线 `~~text~~`、嵌套引用 `>>>` 一次清除、列表标记支持 `+`、粗体改非贪婪匹配（支持内部含特殊符号）。

### 变更
- 斜体规则保留本插件原有的严格模式（未跟随上游改法，上游写法会把 `2*3*4` 类算式误剥为 `234`）；未引入上游的"全局 Markdown 移除"功能（与本插件转图流程冲突，且超出 LLM 输出净化的定位）。

## 0.2.0 (2026-08-17)

### 新增
- 插件配置（`_conf_schema.json`，WebUI 可视化编辑）：
  - `remove_thinking`：移除思考过程开关（默认开）。
  - `remove_markdown`：Markdown 净化开关（默认开）。
  - `t2i`：复杂格式转图配置组——`enable` 总开关，及 `formula`（公式）、`table`（表格，含 HTML 表格）、`code_block`（多行代码块）、`nested_list`（嵌套列表）四个分类开关（均默认开）。

### 变更
- 净化与转图逻辑全部改为按配置执行；配置缺省时行为与旧版一致。

## 0.1.2 (2026-08-17)

### 变更
- 项目命名与说明全面更新以匹配当前功能：展示名改为"LLM输出净化与转图"，描述补充复杂格式自动转图能力；类名 `ThinkingKillerPlugin` 更名为 `LLMPurifierPlugin`，日志前缀统一为 `[LLM Purifier]`；重写 README。

## 0.1.1 (2026-08-17)

### 新增
- 文转图触发条件扩充三类"剥离后难以阅读"的格式：多行代码块（```` ``` ````围栏且内部含换行）、HTML 表格（`<table>`）、多级嵌套列表（缩进的 `-`/`*`/`1.` 列表项）。命中后同样保留排版并走官方文转图整段渲染。

## 0.1.0 (2026-08-17)

### 新增
- 检测 LLM 输出中的数学公式（`$$...$$`、`\[...\]`、`\(...\)`、常见 LaTeX 命令、含命令/上下标的行内 `$...$`）与 Markdown 表格。
- 命中公式/表格时：保留 Markdown 排版（仍移除思考过程），并在发送前对结果打 `use_t2i` 标记，交由 AstrBot 官方文转图流程整段渲染为图片发送（模板跟随 WebUI 中可选的官方 t2i 模板，未引入第三方渲染方式）。

### 变更
- 仅纯文本回复继续执行原有的 Markdown 剥离逻辑，行为不变。
